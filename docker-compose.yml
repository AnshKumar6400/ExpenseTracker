services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend/expense-tracker   # 👈 FIXED
    ports:
      - "80:80"
    depends_on:
      - backend